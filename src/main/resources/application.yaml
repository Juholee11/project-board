# JPA ? ???? ?? ???? ?? ??
# yaml? xml ?? json ?? ???? ?????
#

debug: false # ??? ??? ?? ??? ??? ? ?? ?? ??? ?? ????? ?? false? ?? ??
management.endpoints.web.exposure.include: "*"

logging:
  level:
    com.com.bitstudy.app: debug #??????? ???? ?? ??? debug ??? ???? ??
    org.springframework.web.servlet: debug #??? debug ? ??? ????? ???? false ?? request,reponse ??? ?? ?????
    org.hibernate.type.descriptor.sql.BasicBinder: trace # JAP ??? ?? ? ? ?? ??? ??? ??? ?? ? ? ??? ?? ?? ???? ?????? ? ???? ???. ??? ? ? ?? ????(TODD: ????)

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: bitstudy
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

    # h2 DB를 사용할때
#     url: jdbc:h2:mem:testdb
#     username : sa
#     driver-class-name : org.h2.Driver
  jpa:
    defer-datasource-initialization: true #test? ??????? ???? ?? ? ? ?? ??. ??? resources ?? data.sql(??? ????) ??? ???? DB? ?? ???? ?????? ?? ? ??
    hibernate.ddl-auto: create # 엔티티를 보고 테이블 데이터를 유추해서 자동으로 DDL 데이터를 만들어준다. domain > Article ???? ?? Create table ??? ???? ??? ?? ?? ???? ,Article ? ArticleComment ?????(??? ?????)
    show-sql: true # 디버그 모드에서 sql 문장을 보여줄건지 ?
    properties:
      hibernate.format_sql: true
      hibernate.default_batch_fetch_size: 100


  h2.console.enabled: false # h2 콘솔을
#  h2.console.enabled: true # h2 DB를 사용할때
  sql.init.mode: always # data.sql ? ?? ???? ??? ???? ??
  # naver (?? ????), always(?? ????), embedded(?????)
  #?? ???? ?? ???? ??? ???? ???? ????? ?? ???? always? ????

# data rest 세팅
  data:
    rest:
      base-path: /api # 우리가 엑셀에서 설계한 루트 url 보면 /api로 되어있다 그것 사용하면 됨
      detection-strategy: annotated

      thymeleaf3: # 이름은 @ConfigurationProperties("spring.thymeleaf3") 에서 가져옴
        decoupled-logic: true # false 로 주게되면 thymeleaf 사용 안한다는 의미이다.

